# Advanced example: Factorial with recursion

func factorial(n) {
    if (n < 2) {
        return 1
    }
    return n * factorial(n - 1)
}

print("Factorials from 1 to 10:")
let i = 1
while (i < 11) {
    let result = factorial(i)
    print(result)
    i = i + 1
}
